# OpenClaw Docker Environment Variables
# Copy this file to .env and update values

# Image Configuration
OPENCLAW_IMAGE=openclaw:local

# Directory Configuration (relative to project, or use absolute paths)
OPENCLAW_CONFIG_DIR=./data/config
OPENCLAW_WORKSPACE_DIR=./data/workspace

# Gateway Configuration
OPENCLAW_GATEWAY_TOKEN=your_secure_token_here_change_this
OPENCLAW_GATEWAY_BIND=lan
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790

# Host access (for Claude Code tool — runs via SSH on host)
# HOST_USER=your_username

# Host Filesystem Access
# HOST_HOME_DIR=/home/your_user

# Ollama Configuration
OLLAMA_PORT=11434
OLLAMA_MODEL=Qwen3:14B
# OLLAMA_BASE_URL=server

# LLM Configuration (for OpenAI or any OpenAI-compatible API)
# Set these to override the provider/baseURL/model from openclaw.json
# LLM_PROVIDER=openai
# LLM_BASE_URL=https://api.openai.com/v1
# LLM_MODEL=gpt-4o
# LLM_API_KEY=sk-...
